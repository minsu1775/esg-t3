spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/esgt3
    username: esgt3
    password: esgt3-local
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
