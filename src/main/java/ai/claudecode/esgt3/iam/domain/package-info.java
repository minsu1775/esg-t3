/**
 * IAM 도메인 - ABAC 정책 엔진 핵심.
 *
 * <p>순수 Java(record + 정적 메서드)만 거주한다. Spring·JPA 의존 0.
 * Spring Modulith Named Interface로 외부 모듈에 공개하지 않는다 - 외부는 {@code iam.api}만 사용.
 */
package ai.claudecode.esgt3.iam.domain;
